import 'package:flutter/material.dart';

class ReviewData {
  ReviewData(this.reviewStars, this.reviewCount, this.pointColor);

  final String reviewStars;
  final int reviewCount;
  final Color pointColor;
}
